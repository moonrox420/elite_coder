[pytest]
asyncio_mode = auto
testpaths = tests
# Workspace-local temp root: avoids OS temp dirs (e.g. C:\temp) that may be
# locked down on some Windows environments. (CLI form — `basetemp` is not a
# recognized ini key in pytest 9.)
addopts = --basetemp=.pytest_tmp
